kind = "web"
previewPath = "/"
title = "UpPay Connect"
version = "1.0.0"
id = "artifacts/uppay-connect"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20739

[services.development]
run = "pnpm --filter @workspace/uppay-connect run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/uppay-connect", "run", "build" ]
publicDir = "artifacts/uppay-connect/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20739"
BASE_PATH = "/"
